import { View, Text } from 'react-native'
import React from 'react'
import { Slot, Stack, Tabs } from 'expo-router'

const DashboardLayout = () => {
  return <Tabs />       // automaticly tabs creation
}

export default DashboardLayout